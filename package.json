{
  "name": "awe-axios-workspace",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run clean && tsc -b packages/core packages/mock packages/ioc-aop packages/awe-axios",
    "clean": "rimraf --glob \"packages/*/dist\" \"packages/*/*.tsbuildinfo\" coverage docs/.vitepress/dist .vitepress-cache",
    "test": "vitest run",
    "test:watch": "vitest",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "axios": "^1.11.0",
    "msw": "^2.11.6",
    "reflect-metadata": "^0.2.2",
    "rimraf": "^6.0.1",
    "typescript": "~5.8.3",
    "vitepress": "^2.0.0-alpha.12",
    "vitest": "^3.2.4"
  }
}
