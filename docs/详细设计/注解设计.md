# 注解设计

[[toc]]

## @Api 注解

@Api 注解用来标记一个类为一个 API 类，该注解具有两个功能：

1. 控制反转：将标记的类实例化到工厂
2. 记录类级别配置：运行进行类级别的配置

```typescript
@Api({})
class ExampleApi {
  @Get('/users')
  async getUsers() {
    return await axios.get('/users');
  }
}
```
